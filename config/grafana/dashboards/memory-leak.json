{
  "title": "Memory Leak Detection Dashboard",
  "timezone": "browser",
  "version": 1,
  "panels": [
    {
      "id": 1,
      "title": "Memory Growth Rate (Should be ~0 for healthy apps)",
      "type": "timeseries",
      "datasource": "Prometheus",
      "targets": [
        {
          "expr": "deriv(nodejs_memory_usage_bytes{type=\"heap_used\"}[10m])",
          "legendFormat": "Heap Growth Rate"
        },
        {
          "expr": "deriv(nodejs_memory_usage_bytes{type=\"rss\"}[10m])",
          "legendFormat": "RSS Growth Rate"
        }
      ],
      "alert": {
        "conditions": [
          {
            "query": "A",
            "reducer": { "type": "avg" },
            "evaluator": { "params": [1048576], "type": "gt" }
          }
        ]
      },
      "gridPos": { "h": 8, "w": 12, "x": 0, "y": 0 }
    },
    {
      "id": 2,
      "title": "Memory Leaking Endpoints (Positive = Memory Not Released)",
      "type": "table",
      "datasource": "Prometheus",
      "targets": [
        {
          "expr": "sum by (route, method) (http_endpoint_memory_delta_mb_sum) / sum by (route, method) (http_endpoint_memory_delta_mb_count)",
          "legendFormat": "{{method}} {{route}}",
          "format": "table"
        }
      ],
      "transformations": [
        {
          "id": "organize",
          "options": {
            "excludeByName": { "Time": true },
            "renameByName": {
              "Value": "Avg Memory Delta (MB)"
            }
          }
        }
      ],
      "gridPos": { "h": 8, "w": 12, "x": 12, "y": 0 }
    },
    {
      "id": 3,
      "title": "Garbage Collection Frequency (More GC = Memory Pressure)",
      "type": "timeseries",
      "datasource": "Prometheus",
      "targets": [
        {
          "expr": "rate(nodejs_gc_duration_seconds_count{kind=\"major\"}[5m])",
          "legendFormat": "Major GC/sec"
        },
        {
          "expr": "rate(nodejs_gc_duration_seconds_count{kind=\"minor\"}[5m])",
          "legendFormat": "Minor GC/sec"
        }
      ],
      "gridPos": { "h": 8, "w": 12, "x": 0, "y": 8 }
    },
    {
      "id": 4,
      "title": "Event Loop Lag (High = Memory/CPU Issues)",
      "type": "timeseries",
      "datasource": "Prometheus",
      "targets": [
        {
          "expr": "nodejs_eventloop_lag_seconds * 1000",
          "legendFormat": "Event Loop Lag (ms)"
        }
      ],
      "fieldConfig": {
        "defaults": {
          "unit": "ms",
          "thresholds": {
            "steps": [
              { "color": "green", "value": null },
              { "color": "yellow", "value": 10 },
              { "color": "red", "value": 100 }
            ]
          }
        }
      },
      "gridPos": { "h": 8, "w": 12, "x": 12, "y": 8 }
    },
    {
      "id": 5,
      "title": "Memory Trend Over Time (Should be sawtooth, not climbing)",
      "type": "timeseries",
      "datasource": "Prometheus",
      "targets": [
        {
          "expr": "nodejs_memory_usage_bytes{type=\"heap_used\"} / 1024 / 1024",
          "legendFormat": "Heap Used (MB)"
        }
      ],
      "gridPos": { "h": 8, "w": 24, "x": 0, "y": 16 }
    },
    {
      "id": 6,
      "title": "WebSocket Memory Usage",
      "type": "timeseries",
      "datasource": "Prometheus",
      "targets": [
        {
          "expr": "websocket_memory_usage_bytes",
          "legendFormat": "{{event}}"
        }
      ],
      "gridPos": { "h": 8, "w": 12, "x": 0, "y": 24 }
    },
    {
      "id": 7,
      "title": "WebSocket Memory Delta Distribution",
      "type": "timeseries",
      "datasource": "Prometheus",
      "targets": [
        {
          "expr": "histogram_quantile(0.95, sum(rate(websocket_memory_delta_bytes_bucket[5m])) by (le, event))",
          "legendFormat": "95th percentile {{event}}"
        },
        {
          "expr": "histogram_quantile(0.50, sum(rate(websocket_memory_delta_bytes_bucket[5m])) by (le, event))",
          "legendFormat": "50th percentile {{event}}"
        }
      ],
      "gridPos": { "h": 8, "w": 12, "x": 12, "y": 24 }
    },
    {
      "id": 8,
      "title": "WebSocket Connections",
      "type": "stat",
      "datasource": "Prometheus",
      "targets": [
        {
          "expr": "sum(websocket_connections_total{event=\"connect\"}) - sum(websocket_connections_total{event=\"disconnect\"})",
          "legendFormat": "Active Connections"
        }
      ],
      "gridPos": { "h": 4, "w": 6, "x": 0, "y": 32 }
    },
    {
      "id": 9,
      "title": "WebSocket Message Rate",
      "type": "stat",
      "datasource": "Prometheus",
      "targets": [
        {
          "expr": "sum(rate(websocket_messages_total[1m]))",
          "legendFormat": "Messages/sec"
        }
      ],
      "gridPos": { "h": 4, "w": 6, "x": 6, "y": 32 }
    }
  ],
  "time": {
    "from": "now-1h",
    "to": "now"
  },
  "refresh": "10s"
}
